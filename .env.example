# LiveKit Cloud: dashboard, or `lk cloud auth`
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Direct provider keys. Plugins call these providers directly rather than
# routing through LiveKit Inference, so per-stage timings attribute to one vendor.
DEEPGRAM_API_KEY=
CARTESIA_API_KEY=
GOOGLE_API_KEY=
